# Codeception Test Suite Configuration
#
# Suite for unit or integration tests.

actor: UnitTester
env:
    dev:
    stage:

aspect_mock:
    include_paths:
        - src/*
        - public/*
    temp_folder:
        "unit_tests"

modules:
    enabled:
        - Asserts
        - \Helper\Unit
